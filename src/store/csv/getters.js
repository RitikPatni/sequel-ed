const getters = {
  data(state) {
    return state.data;
  },
  dataHeaders(state) {
    return state.dataHeaders;
  },
  filteredData(state) {
    return state.filteredData
  }
};
export default getters;
